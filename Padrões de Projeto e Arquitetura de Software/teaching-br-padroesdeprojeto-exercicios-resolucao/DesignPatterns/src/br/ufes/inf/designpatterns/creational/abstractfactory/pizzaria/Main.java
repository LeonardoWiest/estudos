package br.ufes.inf.designpatterns.creational.abstractfactory.pizzaria;import java.text.DateFormat;import java.text.SimpleDateFormat;import java.util.Date;import java.text.ParseException;/** * Classe executável. *  * @author Vitor E. Silva Souza (vitorsouza@gmail.com) * @version 2005.07.01 */public class Main {	/** Método principal - executável. */	public static void main(String[] args) {		// Verifica se foi passado um argumento.		if (args.length != 1) {			System.out.println("Use: java Main dd/mm/yyyy");			System.exit(1);		}		// Cria um formatador de datas para ler a data.		Date data = null;		DateFormat df = new SimpleDateFormat("dd/MM/yyyy");		try {			data = df.parse(args[0]);		}		catch (ParseException e) {			System.out.println("A data deve estar no formato dd/mm/yyyy");			System.exit(1);		}		// Cria o cliente e pede uma pizza.		Consumidor consumidor = new Consumidor(data);		consumidor.pedirPizza();	}}